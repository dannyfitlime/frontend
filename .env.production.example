# Frontend production build environment
# For production use your deploy platform's secret envs.

VITE_API_BASE_URL=https://<your-api-domain>

# Optional: only if frontend will call Supabase directly.
VITE_SUPABASE_URL=https://<prod-project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<prod-anon-key>
